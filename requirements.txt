# Dependências do serviço web em deploy (Railway instala a partir deste
# arquivo). Para desenvolvimento local, prefira: pip install -e ".[audio,dev]"

# Transcrição ASR (Whisper local)
faster-whisper>=1.0.0

# API e upload de áudio
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# Voz neural do entrevistador (vozes Microsoft, sem chave de API)
edge-tts>=7.0

# Análise semântica opcional via OpenRouter (cliente compatível com OpenAI)
openai>=1.40
